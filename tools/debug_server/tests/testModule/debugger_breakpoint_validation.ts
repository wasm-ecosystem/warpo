// @ts-nocheck

export function _start(): i32 {
  let value: i32 = 1;
  // This comment has no wasm bytecode mapping.
  let uninitialized: i32;
  value += 1;
  while (true) {
    value += 1;
  }
  return value;
}
