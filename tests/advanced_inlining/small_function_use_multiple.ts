//! { "func": "^.*_start$" }

function smallFunc(a: i32, b: i32): void {
  a + b;
}

export function _start(): void {
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  smallFunc(1, 2);
  return;
}
