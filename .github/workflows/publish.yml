name: publish

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build_and_upload:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm ci
      - uses: ./.github/action/build
        with:
          CMAKE_ARGS: "-DWARPO_RELEASE=ON"
      - name: Set Artifact Name
        id: set_artifact_name
        run: |
          if [ "${{ runner.os }}" == "macOS" ]; then
            echo "OS=macos" >> $GITHUB_OUTPUT
          elif [ "${{ runner.os }}" == "Linux" ]; then
            echo "OS=linux" >> $GITHUB_OUTPUT
          fi
          echo "CPU=$(uname -m)" >> $GITHUB_OUTPUT
      - name: Pack build/warpo directory
        run: |
          tar -czvf build/warpo-${{ github.ref_name }}-${{ steps.set_artifact_name.outputs.OS }}-${{ steps.set_artifact_name.outputs.CPU }}.tar.gz -C build warpo
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/warpo-${{ github.ref_name }}-${{ steps.set_artifact_name.outputs.OS }}-${{ steps.set_artifact_name.outputs.CPU }}.tar.gz
